{
  "name": "grocery-proxy",
  "version": "2.0.0",
  "description": "UK supermarket price comparison API",
  "main": "server.js",
  "scripts": {
    "start": "npx playwright install chromium --with-deps && node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "playwright": "^1.44.0"
  }
}